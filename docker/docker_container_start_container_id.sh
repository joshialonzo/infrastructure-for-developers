# run a container

# docker: main command
# container: subcommand
# start: action
# container_id: value created at runtime
docker container start container_id


# docker: main command
# container: subcommand
# start: action
# --attach: see the logs of the container
# container_id: value created at runtime
docker container start --attach container_id